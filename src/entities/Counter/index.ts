import { counterReducer } from './model/slice/counterSilce';
import { Counter } from './ui/Counter';
import type { CounterSchema } from './model/types/counterSchema';

export { CounterSchema, counterReducer, Counter };
